#######################
#         IRMC        #
#  Bot Configuration  #
#######################
settings:
  enable-verbose: true
  enable-auto-nickchange: true
  enable-auto-server-reconnect: true
  enable-auto-channel-reconnect: true
  enable-auto-split-messages: true
server:
  address: 'irc.esper.net'
  port: 6667
  use-ssl: false    # errors when it's set to true. Fix, ples. http://pastebin.com/2E8iPLiQ
  verify-ssl: false
bot:
  nickname: 'TestBot'
  ident: 'IRMCBot'
  use-nickserv: false
  password: 'password'
ctcp:
  version: '' # wat?
  finger: '' # I ain't fingering no bot.
channels:
  - '#thereverend403'
  - '#BranicYeti'
command-trigger: []
permissions:
  user:
    - permission
  voice:
    - permission
  halfop:
    - permission
  op:
    - permission
  superop:
    - permission
  owner:
    - permission
accesslist: []

