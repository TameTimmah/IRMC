#######################
#         IRMC        #
#  Bot Configuration  #
#######################
settings:
  enable-verbose: false  #Enable for debugging
  enable-auto-nickchange: true
  enable-auto-server-reconnect: true
  enable-auto-channel-reconnect: true
  enable-auto-split-messages: true
server:
  address: 'irc.esper.net'
  port: 6667
  use-ssl: false    # errors when it's set to true. Fix, ples. http://pastebin.com/2E8iPLiQ
  verify-ssl: false
bot:
  nickname: 'IRMCBot'
  ident: 'IRMCBot'
  use-nickserv: false
  password: 'password'
ctcp:
  version: '' # wat?
  finger: '' # I ain't fingering no bot.
channels:
  - '#IRMC'
command-trigger: []
permissions:
  user:
    - permission
  voice:
    - permission
  halfop:
    - permission
  op:
    - permission
  superop:
    - permission
  owner:
    - permission
accesslist: []

